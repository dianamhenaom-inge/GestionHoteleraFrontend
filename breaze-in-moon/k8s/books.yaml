apiVersion: apps/v1
kind: Deployment
metadata:
  name: books-app
  namespace: breaze
spec:
  replicas: 1
  selector:
    matchLabels:
      app: books-app
  template:
    metadata:
      labels:
        app: books-app
    spec:
      initContainers:
        - name: wait-for-mysql
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z books-mysql 3306; do echo "Waiting for books-mysql..."; sleep 3; done']
        - name: wait-for-rooms
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z rooms-app 8080; do echo "Waiting for rooms-app..."; sleep 3; done']
      containers:
        - name: books-app
          image: breaze-books:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://books-mysql:3306/booksdb
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: breaze-secrets
                  key: db-username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: breaze-secrets
                  key: db-password
            - name: SPRING_JPA_HIBERNATE_DDL_AUTO
              value: none
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: breaze-secrets
                  key: jwt-secret
            - name: SERVICE_SECRET
              valueFrom:
                secretKeyRef:
                  name: breaze-secrets
                  key: service-secret
            - name: ROOMS_SERVICE_URL
              value: http://rooms-app:8080
            - name: AUDIT_URL
              value: http://gateway:9090/api/audit/events
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: books-app
  namespace: breaze
spec:
  selector:
    app: books-app
  ports:
    - port: 8080
      targetPort: 8080
